{
  "name": "arc-db-pool",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "hydrate": "npx arc hydrate",
    "hydrate:prisma": "cd ./src/http/get-index; npm install;",
    "prisma:generate": "cd ./src/http/get-index; npx prisma generate;",
    "deploy:no-hyrate": "npx arc --no-hydrate deploy",
    "deploy": "npm run hydrate; npm run hydrate:prisma; npm run deploy:no-hydrate",
    "test": "QUIET=1 tape test/**-test.js | tap-spec"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@architect/architect": "^8.4.11",
    "@architect/functions": "^3.13.9",
    "@architect/sandbox": "^3.3.8",
    "tap-spec": "^5.0.0",
    "tape": "^5.2.2",
    "tiny-json-http": "^7.3.0"
  },
  "devDependencies": {
    "prisma": "^2.18.0"
  }
}
